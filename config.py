import logging

username = ''
password = ''

img_file = 'code.png'

ua = 'Mozilla/5.0 (Linux; U; Android 7.1.1; zh-CN;)'

cookie_file = 'cookies.txt'

log_config = {
    'level': logging.INFO,
    'format': '%(name)s %(asctime)12s %(levelname)s %(message)s',
    'datefmt': '%m-%d %H:%M:%S',
}
